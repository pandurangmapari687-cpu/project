<!-- <!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Counters</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
<div class="d-flex">
  <%- include('./navbar') %>

  <main class="flex-grow-1 p-4" style="margin-left:260px;">
    <h2 class="mb-4">Manage Counters</h2>

    <!-- Create Counter Form -->
    <!-- <div class="card mb-4">
      <div class="card-body">
        <form action="/admin/counter" method="POST" class="row g-3">
          <div class="col-md-4">
            <input type="text" name="name" class="form-control" placeholder="Name" required>
          </div>
          <div class="col-md-2">
            <input type="number" name="value" class="form-control" value="0" placeholder="Initial Value">
          </div>
          <div class="col-md-2">
            <input type="color" name="color" class="form-control form-control-color" value="#7c2bd6">
          </div>
          <div class="col-md-2">
            <input type="number" name="step_inc" class="form-control" value="1" placeholder="Step +">
          </div>
          <div class="col-md-2">
            <input type="number" name="step_dec" class="form-control" value="1" placeholder="Step -">
          </div>
          <div class="col-12">
            <textarea name="description" class="form-control mt-2" placeholder="Description"></textarea>
          </div>
          <div class="col-12 text-end mt-2">
            <button class="btn btn-primary">Save Counter</button>
          </div>
        </form>
      </div>
    </div>

    <!-- Counters List -->
    <!-- <div class="row">
      <% if(counters && counters.length > 0) { %>
        <% counters.forEach(counter => { %>
          <div class="col-md-4 mb-3">
            <div class="card">
              <div class="card-body">
                <h5 style="color: <%= counter.color %>;"><%= counter.name %></h5>
                <p class="display-6"><strong><%= counter.value %></strong></p>
                <p><%= counter.description %></p>
                <div class="d-flex gap-2">
                  <a href="/admin/counter/increase/<%= counter.id %>" class="btn btn-sm btn-success">Increase</a>
                  <a href="/admin/counter/decrease/<%= counter.id %>" class="btn btn-sm btn-warning">Decrease</a>
                  <a href="/admin/counter/delete/<%= counter.id %>" class="btn btn-sm btn-danger" onclick="return confirm('Delete this counter?')">Delete</a>
                </div>
              </div>
            </div>
          </div>
        <% }) %>
      <% } else { %>
        <div class="col-12 text-center text-muted">No counters yet</div>
      <% } %>
    </div>

  </main>
</div> --> -->

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html> -->
